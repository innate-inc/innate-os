# Python dependencies for innate-os ROS2 workspace
# Install with: pip3 install -r pip-requirements.txt
# See ../docs/DEPENDENCIES.md for full analysis of where each package is used

# Use NVIDIA Jetson AI Lab PyPI for optimized PyTorch wheels
# Available indexes: jp6/cu126, jp6/cu128, jp6/cu129
# See: https://pypi.jetson-ai-lab.io/
# NOTE: torch is installed separately in post_update.sh with --index-url to ensure CUDA wheels
# The /+simple/ suffix is required -- it is the PEP 503 index; the bare URL is the
# browsable web UI and 404s for anything the index doesn't host itself
--extra-index-url https://pypi.jetson-ai-lab.io/jp6/cu126/+simple/

# Core
numpy<2
opencv-python

# Networking/API
pydantic
websockets
# aiohttp: the webapp HTTPS front door (webapp/proxy/https_server.py) — static
# serving, /ws rosbridge relay, and the media/settings endpoints.
aiohttp

# Data/ML
h5py
einops
dotenv
# onnx: exports the ACT graph when building the TensorRT engine (manipulation/act_trt.py)
onnx
# onnxruntime: runs vendored Silero VAD and YOLO pose models
onnxruntime
# torch/torchvision/torchaudio are installed separately in post_update.sh
# to ensure CUDA wheels from Jetson AI Lab (pip prefers CPU manylinux wheels otherwise)

# ROS/Robot utilities
urdf-parser-py
dynamixel-sdk
pyserial
trimesh
smbus2
bluezero
python-chess

# Text-to-Speech (via innate-proxy-client)
httpx
websocket-client
aiotools

# Training client dependencies
requests
click
zstandard

# Training manager web UI
fastapi
uvicorn[standard]

# Gemini (Google GenAI)
google-genai

# Input handling
pygame
pynput

# Hot reload
watchdog

# Cupy
cupy-cuda12x

# Debugging
ros-mcp

# Benchmarking / profiling (recorder_benchmark.py)
psutil
