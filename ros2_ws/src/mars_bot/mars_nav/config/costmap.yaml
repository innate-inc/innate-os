navigation:
  global_costmap:
    global_costmap:
      ros__parameters:
        update_frequency: 5.0
        publish_frequency: 5.0
        global_frame: map
        robot_base_frame: base_link
        use_sim_time: false
        footprint: "[[0.25, 0.165], [0.25, -0.165], [-0.20, -0.165], [-0.20, 0.165]]"
        resolution: 0.05
        track_unknown_space: true        # keep unknown cells distinct (NO_INFORMATION)
        unknown_cost_value: 255          # 255 = NO_INFORMATION (not lethal); planner treats it per allow_unknown
        rolling_window: false
        plugins: ["static_layer", "obstacle_layer", "inflation_layer"]
        filters: ["keepout_filter", "keepout_inflation"]
        static_layer:
          plugin: "nav2_costmap_2d::StaticLayer"
          map_subscribe_transient_local: True
          map_topic: "/map"
        obstacle_layer:
          plugin: "nav2_costmap_2d::ObstacleLayer"
          enabled: True
          observation_sources: scan
          scan:
            topic: /scan
            max_obstacle_height: 2.0
            clearing: True
            marking: True
            data_type: "LaserScan"
            raytrace_max_range: 3.0
            raytrace_min_range: 0.2
            obstacle_max_range: 2.5
            obstacle_min_range: 0.2
        inflation_layer:
          plugin: "nav2_costmap_2d::InflationLayer"
          cost_scaling_factor: 10.0
          inflation_radius: 0.3
        keepout_filter:
          plugin: "nav2_costmap_2d::KeepoutFilter"
          enabled: True
          filter_info_topic: "/nav/keepout_filter_info"
        keepout_inflation:
          plugin: "nav2_costmap_2d::InflationLayer"
          cost_scaling_factor: 10.0
          inflation_radius: 0.3
        always_send_full_costmap: False


local_costmap:
  local_costmap:
    ros__parameters:
      update_frequency: 5.0
      publish_frequency: 5.0
      global_frame: odom
      robot_base_frame: base_link
      use_sim_time: false
      rolling_window: true
      width: 6
      height: 6
      resolution: 0.05
      footprint: "[[0.25, 0.165], [0.25, -0.165], [-0.20, -0.165], [-0.20, 0.165]]"
      track_unknown_space: false        # unknown space is treated as FREE in the rolling local costmap
      unknown_cost_value: 255          # unused while track_unknown_space is false
      # Depth obstacle layer (voxel_layer/STVL) intentionally NOT loaded: with a
      # calibrated camera it marks phantom obstacles from floor leakage
      # (measured 2x cross-track degradation + swerve spikes, R7-27 2026-07).
      # Re-add "voxel_layer" here only after the depth pipeline passes QA.
      plugins: [
        "obstacle_layer",
        "inflation_layer",
        ]
      filters: ["keepout_filter", "keepout_inflation"]
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: True
        observation_sources: scan
        scan:
          topic: /scan
          max_obstacle_height: 2.0
          clearing: True
          marking: True
          data_type: "LaserScan"
          raytrace_max_range: 3.0
          raytrace_min_range: 0.2
          obstacle_max_range: 2.5
          obstacle_min_range: 0.2
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 10.0
        inflation_radius: 0.25
      keepout_filter:
        plugin: "nav2_costmap_2d::KeepoutFilter"
        enabled: True
        filter_info_topic: "/nav/keepout_filter_info"
      keepout_inflation:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 10.0
        inflation_radius: 0.25
      voxel_layer:
        # NOTE: this layer is silently inert when the robot has no stereo
        # calibration (/mars/main_camera/points never publishes) — nav is then
        # lidar-only. mode_manager logs a warning when that is the case.
        # expected_update_rate is left at 0 on purpose: a nonzero value marks
        # the whole costmap not-current when the camera is down, which blocks
        # planning entirely.
        plugin: "spatio_temporal_voxel_layer/SpatioTemporalVoxelLayer"
        enabled:               true
        voxel_decay:           2.0     #seconds if linear, e^n if exponential
        decay_model:           0      #0=linear, 1=exponential, -1=persistent
        voxel_size:            0.05   #meters
        track_unknown_space:   true   #default space is unknown
        observation_persistence: 0.0  #seconds
        max_obstacle_height:   0.36    #meters
        mark_threshold:        0      #voxel height
        update_footprint_enabled: true
        combination_method:    1      #1=max, 0=override
        obstacle_range:        3.0    #meters
        origin_z:              0.0    #meters
        publish_voxel_map:     false   # default off
        transform_tolerance:   0.2    # seconds
        observation_sources:   rgbd1_clear rgbd1_mark
        rgbd1_mark:
          data_type: PointCloud2
          topic: /mars/main_camera/points
          marking: true
          clearing: false
          obstacle_range: 2.5
          min_obstacle_height: 0.05     #default 0, meters
          max_obstacle_height: 0.36     #defaule 3, meters
          expected_update_rate: 0.0    #default 0, if not updating at this rate at least, remove from buffer
          observation_persistence: 0.0 #default 0, use all measurements taken during now-value, 0=latest 
          inf_is_valid: false          #default false, for laser scans
          clear_after_reading: false    #default false, clear the buffer after the layer gets readings from it
          filter: "voxel"              #default passthrough, apply "voxel", "passthrough", or no filter to sensor data, recommended to have at one filter on
          voxel_min_points: 5          #default 0, minimum points per voxel for voxel filter
        rgbd1_clear:
          enabled: false                #default true, can be toggled on/off with associated service call
          data_type: PointCloud2
          topic: /mars/main_camera/points
          marking: false
          clearing: true
          min_z: 0.0                   #default 0, meters
          max_z: 7.0                   #default 10, meters
          vertical_fov_angle: 0.7      #default 0.7, radians
          horizontal_fov_angle: 1.04   #default 1.04, radians
          decay_acceleration: 1.       #default 0, 1/s^2. If laser scanner MUST be 0
          model_type: 0                #default 0 (depth camera). Use 1 for 3D Lidar

 




mapfree:
  global_costmap:
    global_costmap:
      ros__parameters:
        update_frequency: 5.0
        publish_frequency: 5.0
        global_frame: odom
        robot_base_frame: base_link
        use_sim_time: false
        footprint: "[[0.25, 0.165], [0.25, -0.165], [-0.20, -0.165], [-0.20, 0.165]]"
        resolution: 0.05
        # track_unknown_space: false        # Now unknown areas are treated as obstacles
        # unknown_cost_value: 255          # Unknown cells set to a lethal cost
        rolling_window: true
        width: 10
        height: 10
        plugins: [ 
          "obstacle_layer", 
          "inflation_layer",
          ]
        obstacle_layer:
          plugin: "nav2_costmap_2d::ObstacleLayer"
          enabled: True
          observation_sources: scan
          scan:
            topic: /scan
            max_obstacle_height: 2.0
            clearing: True
            marking: True
            data_type: "LaserScan"
            raytrace_max_range: 3.0
            raytrace_min_range: 0.2
            obstacle_max_range: 2.5
            obstacle_min_range: 0.2
        inflation_layer:
          plugin: "nav2_costmap_2d::InflationLayer"
          cost_scaling_factor: 10.0
          inflation_radius: 0.35
        always_send_full_costmap: False
        voxel_layer:
          # NOTE: silently inert without stereo calibration — see the comment
          # on the local costmap's voxel_layer.
          plugin: "spatio_temporal_voxel_layer/SpatioTemporalVoxelLayer"
          enabled:               true
          voxel_decay:           2.0     #seconds if linear, e^n if exponential
          decay_model:           0      #0=linear, 1=exponential, -1=persistent
          voxel_size:            0.05   #meters
          track_unknown_space:   true   #default space is unknown
          observation_persistence: 0.0  #seconds
          max_obstacle_height:   0.36    #meters
          mark_threshold:        0      #voxel height
          update_footprint_enabled: true
          combination_method:    1      #1=max, 0=override
          obstacle_range:        3.0    #meters
          origin_z:              0.0    #meters
          publish_voxel_map:     false   # default off
          transform_tolerance:   0.2    # seconds
          observation_sources:   rgbd1_clear rgbd1_mark
          rgbd1_mark:
            data_type: PointCloud2
            topic: /mars/main_camera/points
            marking: true
            clearing: false
            obstacle_range: 2.5
            min_obstacle_height: 0.05     #default 0, meters
            max_obstacle_height: 0.36     #defaule 3, meters
            expected_update_rate: 0.0    #default 0, if not updating at this rate at least, remove from buffer
            observation_persistence: 0.0 #default 0, use all measurements taken during now-value, 0=latest 
            inf_is_valid: false          #default false, for laser scans
            clear_after_reading: false    #default false, clear the buffer after the layer gets readings from it
            filter: "voxel"              #default passthrough, apply "voxel", "passthrough", or no filter to sensor data, recommended to have at one filter on
            voxel_min_points: 5          #default 0, minimum points per voxel for voxel filter
          rgbd1_clear:
            enabled: false                #default true, can be toggled on/off with associated service call
            data_type: PointCloud2
            topic: /mars/main_camera/points
            marking: false
            clearing: false
            min_z: 0.0                   #default 0, meters
            max_z: 7.0                   #default 10, meters
            vertical_fov_angle: 0.7      #default 0.7, radians
            horizontal_fov_angle: 1.04   #default 1.04, radians
            decay_acceleration: 1.       #default 0, 1/s^2. If laser scanner MUST be 0
            model_type: 0                #default 0 (depth camera). Use 1 for 3D Lidar

 
