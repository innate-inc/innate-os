# Files intentionally allowed past the CI file-size limit (100 KiB).
#
# Uses gitignore-style patterns (globs, anchoring with a leading "/",
# directory matches with a trailing "/", "!" to un-ignore). Adding an entry
# here is the explicit approval for a large file — it shows up in the diff
# and gets reviewed like any other change.
#
# Note: only files *changed* in a pull request are checked, so files already
# committed (existing meshes, readme assets, ...) are grandfathered in and do
# not need to be listed unless a PR modifies them.
#
# Enforced by .github/workflows/file-size-check.yaml

# Generated lockfiles — large but legitimate, and reviewed as a unit.
uv.lock
package-lock.json
